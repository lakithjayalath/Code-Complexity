package Main;


import java.io.BufferedReader;
import java.io.FileReader;
import java.io.IOException;
import java.util.ArrayList;

public class ReadFileLineByLineUsingBufferedReader {

	/*
	 * public static void main(String[] args) { BufferedReader reader;
	 * ArrayList<String> data = new ArrayList<String>(100); try { reader = new
	 * BufferedReader(new FileReader( "C:/Users/L/Desktop/sample.txt")); String line
	 * = reader.readLine(); while (line != null) { //System.out.println(line);
	 * 
	 * String input = line; boolean isPresent = input.indexOf("extends") != -1 ?
	 * true : false; if (isPresent) {
	 * 
	 * 
	 * String[] sp = input.split(" "); System.out.println("second word is " +
	 * sp[1]); System.out.println("third word is " + sp[2]);
	 * System.out.println("Fourth words is " +sp[3]);
	 * 
	 * 
	 * if(sp[1].equals("class")) { System.out.println("third word is " + sp[2]); }
	 * else { System.out.println("No offence"); }
	 * 
	 * data.add(input); for(int i = 0; i < data.size(); i++) {
	 * System.out.println(data.get(i)); } }
	 * 
	 * 
	 * System.out.println("Doing search with different case"); isPresent =
	 * input.indexOf("extends") != -1 ? true : false;
	 * System.out.println("isPresent: " + isPresent);
	 * 
	 * 
	 * // read next line line = reader.readLine();
	 * 
	 * 
	 * 
	 * } reader.close(); } catch (IOException e) { e.printStackTrace(); } }
	 */
}